# Goal Tracker

Приложение для управления целями, задачами и привычками с MongoDB бэкендом.

## 🚀 Запуск приложения

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка MongoDB

#### Локальный MongoDB
```bash
# Установите MongoDB локально или используйте Docker
docker run -d -p 27017:27017 --name mongodb mongo
```

#### MongoDB Atlas (облачный)
1. Создайте аккаунт на [MongoDB Atlas](https://www.mongodb.com/atlas)
2. Создайте новый кластер
3. Получите строку подключения
4. Создайте файл `.env` в корне проекта:

```env
VITE_MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/goaltracker?retryWrites=true&w=majority
VITE_MONGODB_DB=goaltracker
```

### 3. Запуск приложения
```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

## 🏗️ Архитектура

### Фронтенд
- **React 18** с TypeScript
- **Vite** для сборки
- **Tailwind CSS** для стилизации
- **Zustand** для управления состоянием
- **React Hook Form + Zod** для валидации форм
- **React Router** для навигации
- **Recharts** для графиков

### Бэкенд
- **MongoDB** как основная база данных
- **MongoDB Driver** для работы с БД
- **Сервисный слой** для бизнес-логики
- **Типизированные коллекции** для безопасности данных

### Структура проекта
```
src/
├── components/          # UI компоненты
├── components/forms/     # Формы
├── hooks/              # React хуки
├── lib/                # Утилиты и MongoDB
├── pages/              # Страницы приложения
├── services/           # Сервисы MongoDB
├── stores/             # Zustand сторы
├── types/              # TypeScript типы
└── main.tsx           # Точка входа
```

## 📊 Функционал

### 🎯 Цели
- Создание, редактирование, удаление
- Этапы и задачи
- Прогресс по задачам или метрикам
- Категории и приоритеты
- Дедлайны (4 типа)

### 📝 Задачи
- Подзадачи
- Приоритеты и сложность
- Вес вклада в прогресс
- Периодические задачи

### 📊 Метрики (Привычки/Счётчики)
- Два типа: привычки и счётчики
- Запись прогресса с шагом или вручную
- Аналитика с графиками
- Серии и рекорды

### 📅 Календарь
- Месячный вид
- Фильтрация по типам сущностей
- Отображение задач, целей, метрик

### 📈 Аналитика
- Статистика по целям
- Графики прогресса
- Тепловые карты активности

## 🔐 Демо доступ

- **Email**: demo@example.com
- **Пароль**: demo123

## 🗄️ Экспорт данных

Все данные можно экспортировать в JSON формате через страницу настроек.

## 🔄 Миграция данных

Приложение поддерживает миграцию данных из localStorage в MongoDB для плавного перехода.

## 🌐 Деплой

### Vercel
1. Подключите репозиторий к Vercel
2. Настройте переменные окружения в Vercel Dashboard
3. Деплой произойдет автоматически

### Netlify
1. Подключите репозиторий к Netlify
2. Настройте переменные окружения
3. Деплой произойдет автоматически

## 📝 Лицензия

MIT License
